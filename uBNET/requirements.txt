# uBNET Dependencies
#
# Note: GTK and AppIndicator bindings are system packages
# and cannot be installed via pip. Install them with:
#
#   sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-3.0 \
#       gir1.2-ayatanaappindicator3-0.1
#
# This file is provided for documentation purposes.
# Use install.sh for proper installation.

# The application uses these Python standard library modules:
# - json
# - pathlib
# - dataclasses
# - time

# PyGObject (reference only - install via apt)
# PyGObject>=3.42.0
