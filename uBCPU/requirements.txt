# uBCPU Requirements
# Most dependencies are system packages installed via apt

# System packages (install with apt):
# sudo apt install python3 python3-gi gir1.2-ayatanaappindicator3-0.1 libnotify-bin

# No pip packages required - uses system GTK/GI bindings
